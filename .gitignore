# Dependencies
node_modules/

# Hardhat auto-generated (rebuild with: npx hardhat compile / test)
cache/
artifacts/
typechain-types/

# Reports — raw auto-generated outputs (reports/*.md are committed, raw txt is not)
reports/gas-report.txt
coverage/
coverage.json

# Trash folder — local scratch only, never commit
trash/

# Dotenv file
.env

# Frontend generated files (written by deploy script at runtime)
frontend/public/deployment.json
frontend/public/abi.json
